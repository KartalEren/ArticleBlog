@model AuthorDetailDTO

<div class="container-xxl flex-grow-1 container-p-y">

    <div style="height:100px;"></div>

    <div class="row">
        <div class="col-md-12">
            <div class="card mb-4">

                <h5 class="card-header"> <i class='bx bxs-user-circle'></i> @Model.FirstName @Model.LastName Profile </h5>

                <hr class="my-0">
                <div class="card-body">
                    <div class="row">

                        <div class="mb-3 col-md-6 mt-3">
                            <label asp-for="FirstName" class="form-label"> <i class='bx bx-child'></i> FirstName</label>
                            <input asp-for="FirstName" class="form-control" type="text" autofocus="" disabled>
                        </div>

                        <div class="mb-3 col-md-6 my-3">
                            <label asp-for="LastName" class="form-label"> <i class='bx bx-child'></i> LastName</label>
                            <input asp-for="LastName" class="form-control" type="text" autofocus="" disabled>
                        </div>

                        <div class="mb-3 col-md-6">
                            <label asp-for="Email" class="form-label"> <i class='bx bx-envelope'></i> Email</label>
                            <input asp-for="Email" class="form-control" type="email" autofocus="" disabled>
                        </div>

                        <div class="mb-3 col-md-6">
                            <label asp-for="PhoneNumber" class="form-label"> <i class='bx bx-phone'></i> Phone Number</label>
                            <input asp-for="PhoneNumber" class="form-control" type="text" autofocus="" disabled>
                        </div>

                        <div class="mb-3 col-md-6">
                            <label asp-for="About" class="form-label"> <i class='bx bxs-id-card'></i> About</label>
                            <textarea asp-for="About" class="form-control" type="text" autofocus="" disabled></textarea>
                        </div>
                    </div>
                </div>

               @*  <div class="col-12">

                    <h5 class="card-header"> <i class='bx bx-book'></i> @Model.Email Articles </h5>

                    <table class="table table-bordered mt-3" id="articlesTable" width="100%" collspacing="0">
                        <thead>
                            <tr>
                                <th>Title</th>
                                <th>Category</th>
                                <th>Created Date</th>
                                <th>Content</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody class="table-border-bottom-0">
                            @foreach (var item in Model.Articles)
                            {
                                <tr name="@item.ID">
                                    <td> @item.Title </td>
                                    <td> @item.Category.CategoryName </td>
                                    <td> @item.Content </td>
                                    <td> @item.CreatedDate.ToShortDateString() </td>
                                    <td>
                                        <a class="btn btn-dark btn-sm" data-id="@item.ID" asp-controller="Home" asp-action="Detail" asp-route-id="@item.ID"> Read More </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div> *@

                <div class="mt-4">
                    <a asp-controller="Home" asp-action="Index" type="submit" class="btn btn-dark me-2" > Back To HamePage </a>
                </div>

            </div>
        </div>
    </div>
</div>
